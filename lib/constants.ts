// Constants for API configuration
export const WAGER_WINDOW_START = "2025-12-26";

// Roobet API key is stored as an environment variable
// Access via process.env.ROOBET_API_KEY
// (ACEBET_API_TOKEN is legacy/unused — the leaderboard API is now Roobet-backed)
